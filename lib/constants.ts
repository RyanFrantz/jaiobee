// Stores a user's access token.
const ACCESS_COOKIE = "access";
// Stores a user's refresh token.
const REFRESH_COOKIE = "refresh";
// Stores a user's locale and timezone.
const LOCALE_COOKIE = "locale";

export { ACCESS_COOKIE, LOCALE_COOKIE, REFRESH_COOKIE };
